<resources>
    <!--
        Base application theme for light mode.

        This theme inherits from the MaterialComponents DayNight theme and removes the default
        ActionBar. This ensures a consistent base theme for the app while allowing
        Jetpack Compose to manage the entire screen layout, including the app bar.
    -->
    <style name="Theme.GustoLunchMenu" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>
</resources>
